# Fuzz Target: formatter_fuzz
#
# Feeds arbitrary byte strings to the Axon formatter.
# The formatter must never panic on any input.
#
# cargo-fuzz target (to be integrated):
#
# #![no_main]
# use libfuzzer_sys::fuzz_target;
#
# fuzz_target!(|data: &[u8]| {
#     if let Ok(s) = std::str::from_utf8(data) {
#         let _ = axonc::fmt::Formatter::format(s, "fuzz.axon");
#     }
# });
