# Fuzz Target: parser_fuzz
#
# Feeds arbitrary byte strings through lex → parse.
# The parser must never panic on any input.
#
# cargo-fuzz target (to be integrated):
#
# #![no_main]
# use libfuzzer_sys::fuzz_target;
#
# fuzz_target!(|data: &[u8]| {
#     if let Ok(s) = std::str::from_utf8(data) {
#         let _ = axonc::parse_source(s, "fuzz.axon");
#     }
# });
