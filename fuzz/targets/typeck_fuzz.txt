# Fuzz Target: typeck_fuzz
#
# Full lex → parse → typecheck on arbitrary input.
# The type checker must never panic on any input.
#
# cargo-fuzz target (to be integrated):
#
# #![no_main]
# use libfuzzer_sys::fuzz_target;
#
# fuzz_target!(|data: &[u8]| {
#     if let Ok(s) = std::str::from_utf8(data) {
#         let _ = axonc::typeck::check(s, "fuzz.axon");
#     }
# });
