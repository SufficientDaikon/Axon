# Fuzz Target: lexer_fuzz
#
# Feeds arbitrary byte strings to the Axon lexer.
# The lexer must never panic on any input.
#
# cargo-fuzz target (to be integrated):
#
# #![no_main]
# use libfuzzer_sys::fuzz_target;
#
# fuzz_target!(|data: &[u8]| {
#     if let Ok(s) = std::str::from_utf8(data) {
#         let mut lexer = axonc::lexer::Lexer::new(s, "fuzz.axon");
#         let _tokens = lexer.tokenize();
#     }
# });
