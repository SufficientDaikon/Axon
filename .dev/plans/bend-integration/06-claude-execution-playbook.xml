<?xml version="1.0" encoding="UTF-8"?>
<axonBendExecutionPlaybook>
  <metadata>
    <purpose>Claude Opus 4.6-optimized execution protocol for Bend integration phases. Tailored for Claude Code agentic environment with tool-aware patterns.</purpose>
    <targetModel>claude-opus-4-6</targetModel>
    <environment>Claude Code (VS Code / CLI) with Bash, Read, Edit, Write, Grep, Glob, Agent tools</environment>
  </metadata>

  <executionPrinciples>
    <principle id="EP1">Work in phases. Never attempt more than one workstream in a single context window.</principle>
    <principle id="EP2">Compact context before starting each new phase. Use /compact or summarize to handoff packet.</principle>
    <principle id="EP3">Run cargo test after every file modification. Never proceed with failing tests.</principle>
    <principle id="EP4">Use Agent tool (subagent_type="Explore") for codebase discovery and Agent tool (subagent_type="general-purpose") for parallel file creation. Keep main context for decisions, sequencing, and code integration.</principle>
    <principle id="EP5">Create each new module with tests immediately. Never defer tests to "later".</principle>
    <principle id="EP6">Pin non-negotiables in every prompt: test baseline 1034+, zero warnings, project path h:/programing language/axon/.</principle>
    <principle id="EP7">If context approaches 80K tokens, stop, create handoff packet, and start new session.</principle>
    <principle id="EP8">Maximize parallel tool calls: when creating multiple independent files, use multiple Write/Edit calls in a single message. When searching for multiple patterns, launch parallel Grep calls.</principle>
    <principle id="EP9">Use TodoWrite to track epic/story progress. Mark tasks in_progress before starting and completed immediately after finishing. Only one task should be in_progress at a time.</principle>
  </executionPrinciples>

  <toolUsagePatterns>
    <pattern name="CodebaseDiscovery">
      <when>Starting a new epic or needing to understand existing code structure</when>
      <how>Launch Agent(subagent_type="Explore") with a targeted question. The Explore agent uses Grep/Glob/Read internally and returns a summary, keeping main context clean.</how>
      <example>Agent(prompt="Find all places in src/typeck.rs where errors are returned early (Result::Err, ? operator). Report line numbers and the error types.")</example>
    </pattern>
    <pattern name="ParallelFileCreation">
      <when>An epic requires creating 2+ independent new files</when>
      <how>Use multiple Write tool calls in a single message. All writes execute in parallel.</how>
      <example>Write src/diagnostics.rs + Write tests/diagnostics_tests.rs + Write src/mir/transform/mod.rs in one message</example>
    </pattern>
    <pattern name="TargetedSearch">
      <when>Looking for specific patterns in the codebase</when>
      <how>Use Grep directly with specific regex patterns. Use Glob for file discovery. Only read files you need to modify.</how>
      <avoid>Never Read entire large files (parser.rs is 2100+ lines). Use Read with offset/limit for specific sections.</avoid>
    </pattern>
    <pattern name="TestAfterChange">
      <when>After every Edit or Write to a source file</when>
      <how>Run Bash("cargo test 2>&amp;1 | tail -5") for quick pass/fail. Only read full output on failure.</how>
    </pattern>
    <pattern name="IncrementalBuilds">
      <when>Verifying compilation during development</when>
      <how>Run Bash("cargo build 2>&amp;1 | head -20") first for rapid feedback. Full cargo test only after build succeeds.</how>
    </pattern>
  </toolUsagePatterns>

  <phaseProtocol>
    <phase id="ALPHA">
      <contextBudget>80K tokens</contextBudget>
      <sessionEstimate>2-3 sessions</sessionEstimate>
      <entryCondition>No preconditions. This is the first phase.</entryCondition>
      <exitCondition>All 1,034+ tests pass. Diagnostics accumulate. PassManager operational. 100+ snapshots. Handoff packet created.</exitCondition>
      <handoffPacket>
        <field>filesCreated (list of new files with line counts)</field>
        <field>filesModified (list with summary of changes)</field>
        <field>testsAdded (count and categories)</field>
        <field>testBaseline (new total test count)</field>
        <field>openRisks (any unresolved issues)</field>
        <field>nextActions (what BETA should do first)</field>
      </handoffPacket>
    </phase>

    <phase id="BETA">
      <contextBudget>80K tokens per workstream</contextBudget>
      <sessionEstimate>4-6 sessions (1-2 per workstream WS3/WS4/WS5)</sessionEstimate>
      <entryCondition>ALPHA handoff packet loaded. PassManager confirmed operational.</entryCondition>
      <exitCondition>Fusion 3x verified. Parallelism 2x verified. Fold/reduce functional. 200+ new tests.</exitCondition>
      <handoffPacket>
        <field>filesCreated (list of new files with line counts)</field>
        <field>filesModified (list with summary of changes)</field>
        <field>testsAdded (count and categories)</field>
        <field>testBaseline (new total test count)</field>
        <field>benchmarkResults (fusion throughput, parallelism scaling factors)</field>
        <field>openRisks (any unresolved issues)</field>
        <field>nextActions (what GAMMA should do first)</field>
      </handoffPacket>
    </phase>

    <phase id="GAMMA">
      <contextBudget>80K tokens</contextBudget>
      <sessionEstimate>2-3 sessions</sessionEstimate>
      <entryCondition>BETA handoff packet loaded. Fusion and parallelism confirmed working.</entryCondition>
      <exitCondition>Linear types enforced. Zero-copy verified on 5+ patterns. ADT layouts benchmarked.</exitCondition>
      <handoffPacket>
        <field>filesCreated (list of new files with line counts)</field>
        <field>filesModified (list with summary of changes)</field>
        <field>testsAdded (count and categories)</field>
        <field>testBaseline (new total test count)</field>
        <field>linearityPatterns (list of verified zero-copy patterns)</field>
        <field>adtBenchmarks (layout comparison results)</field>
        <field>openRisks (any unresolved issues)</field>
        <field>nextActions (what DELTA should do first)</field>
      </handoffPacket>
    </phase>

    <phase id="DELTA">
      <contextBudget>60K tokens</contextBudget>
      <sessionEstimate>1-2 sessions</sessionEstimate>
      <entryCondition>GAMMA handoff packet loaded.</entryCondition>
      <exitCondition>with-blocks working. Docs updated. Math-notation designed.</exitCondition>
      <handoffPacket>
        <field>filesCreated (list of new files with line counts)</field>
        <field>filesModified (list with summary of changes)</field>
        <field>testsAdded (count and categories)</field>
        <field>testBaseline (new total test count)</field>
        <field>docsUpdated (list of documentation changes)</field>
        <field>openRisks (any unresolved issues)</field>
        <field>nextActions (what EPSILON should do first)</field>
      </handoffPacket>
    </phase>

    <phase id="EPSILON">
      <contextBudget>40K tokens</contextBudget>
      <sessionEstimate>1 session</sessionEstimate>
      <entryCondition>DELTA handoff packet loaded. All DELTA gates passed — with-blocks, docs, and math-notation design document are complete.</entryCondition>
      <exitCondition>Design documents written and reviewed. No code changes.</exitCondition>
      <handoffPacket>
        <field>designDocsCreated (list of design documents with summaries)</field>
        <field>openQuestions (unresolved design decisions)</field>
        <field>futureWork (items for post-Bend-integration roadmap)</field>
      </handoffPacket>
    </phase>
  </phaseProtocol>

  <errorRecoveryProtocol>
    <scenario name="ContextOverflow">
      <symptom>Error during execution messages, sluggish responses, or repeated tool failures suggesting context limits.</symptom>
      <action>Immediately stop. Run /compact. Create handoff packet. Resume in new session with packet.</action>
    </scenario>

    <scenario name="TestRegression">
      <symptom>Existing test fails after code change.</symptom>
      <action>Do NOT proceed. Diagnose root cause. Fix regression before any new work. If fix requires major refactor, create risk entry in handoff packet.</action>
    </scenario>

    <scenario name="CompilationFailure">
      <symptom>cargo build fails.</symptom>
      <action>Read error output carefully. Fix in smallest possible diff. Run cargo test after fix. Do not accumulate multiple unfixed errors.</action>
    </scenario>

    <scenario name="FusionIncorrectness">
      <symptom>Fused output differs from unfused by more than epsilon.</symptom>
      <action>Disable fusion for that pattern. Add failing test. Debug numerical issue. Do not ship incorrect fusion.</action>
    </scenario>

    <scenario name="ParallelismDeadlock">
      <symptom>Test hangs indefinitely or produces non-deterministic failures.</symptom>
      <action>Kill process. Revert to sequential execution for affected pass. Add timeout to parallel tests. Investigate dependency graph for cycles.</action>
    </scenario>

    <scenario name="LinkerShadowing">
      <symptom>Unexpected linker errors on Windows — Git's link.exe shadows MSVC linker.</symptom>
      <action>Set LINK environment variable to point to MSVC's link.exe. Verify with where link.exe before retrying.</action>
    </scenario>
  </errorRecoveryProtocol>

  <sessionStartChecklist>
    <step order="1">Load handoff packet from previous phase (if not ALPHA).</step>
    <step order="2">Verify project path: h:/programing language/axon/</step>
    <step order="3">Run cargo test and confirm baseline test count matches handoff packet.</step>
    <step order="4">Run cargo build and confirm zero warnings.</step>
    <step order="5">Load only the plan documents relevant to the current workstream.</step>
    <step order="6">Create todo list for the session's deliverables.</step>
    <step order="7">Begin work on first task.</step>
  </sessionStartChecklist>

  <sessionEndChecklist>
    <step order="1">Run cargo test — all tests must pass.</step>
    <step order="2">Run cargo build — zero warnings required.</step>
    <step order="3">Create handoff packet with all required fields.</step>
    <step order="4">Summarize what was accomplished and what remains.</step>
    <step order="5">List any open risks or blockers for next session.</step>
  </sessionEndChecklist>
</axonBendExecutionPlaybook>
