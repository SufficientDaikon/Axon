fn is_even(n: Int64) -> Bool {
    if n == 0 {
        return true;
    }
    return is_odd(n - 1);
}

fn is_odd(n: Int64) -> Bool {
    if n == 0 {
        return false;
    }
    return is_even(n - 1);
}

fn main() {
    let a: Bool = is_even(4);
    let b: Bool = is_odd(3);
}
